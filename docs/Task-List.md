# Task List（任务清单）

## 已完成

- 增强 MySQL→SQL Server 转换器的语法清理与类型映射
- 处理超长 SQL 与不兼容类型（`json`、`varchar(>4000)` 等）
- 调整索引与约束策略以提升全量初始化稳定性
- 通过 Docker Compose 跑通“初始化数据库 → 启动后端 → 启动前端”闭环
- 补齐中文架构、设计、开发、测试、用户手册与根 README 文档

## 可选增强（未纳入本次交付硬性范围）

- 生产化约束回补：在数据清洗后恢复唯一约束与外键
- 初始化性能优化：对大表插入采用批处理或 `bcp`/`BULK INSERT` 方案
- 数据类型精细化：对 JSON 字段增加约束与索引策略（如计算列 + 索引）

